To run: 
1. Go to the source code folder
2. On windows run this command: "mvnw spring-boot:run". On mac run this command: "./mvnw spring-boot:run"
3. The 2 Endpoints will be available at http://localhost:8080.
